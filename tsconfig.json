{
    "compilerOptions": {
        "target": "es2015",
        "module": "commonjs",

        "strict": true,
        "noImplicitAny": true,
        "removeComments": true,
        "sourceMap": true,
        "noUnusedLocals": true,
        "skipLibCheck": true, // added per ai instructions, remove if breaks
        "outDir": "js",
        "baseUrl": "./",
        "paths": {
            "~/*" : ["src/*"] // resolve any `~/foo/bar` to `<baseUrl>/src/foo/bar`
        }
    },
    "include": [
     // disabled   "node_modules/@types/**/*.d.ts",
        "src/**/*.ts",
        "src/**/*.d.ts"
    ]
}
